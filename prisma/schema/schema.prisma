generator webClient {
  provider        = "prisma-client-js"
  output          = "client/web"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource webDb {
  provider = "mysql"
  url      = env("WEB_DATABASE_URL")
}
